# AGENTS.md — 在这个仓库里干活的规矩

这份文件写给**在这个仓库里被派活干活的 agent**（不论你跑在哪台机器上）。目标只有一个：让你交出来的东西**能被直接评审和合并**，而不是让我重做一遍。

产品、人物画像、产品律、决策日志在 `gui/ROADMAP.md`；Rust↔前端契约在 `gui/CONTRACT.md`。**这两份是权威**，冲突时以它们为准，本文件只补"干活时必须知道的"。

---

## 1. 这是什么产品（30 秒）

Cante 是一个给**中国非 IT 办公用户**（画像：王姐，45 岁，行政/财务，Windows，只会复制粘贴和求和 Excel，微信 PC 常开）用的桌面应用。**只有一个界面**（简单模式），32 张任务卡，每张卡背后是一段中文提示词，交给助手去执行。

**三条产品律**（每个决定都要回答它们）：
1. **把麻烦事做完**（不是"看起来做了"）；
2. **别弄坏我的东西**（结果永不覆盖原文件；破坏性动作先问人；一切可撤销）；
3. **出错能看懂并有出路**（发生了什么 + 你可以怎么做 + 复制详情）。

**两条红线**：
- **微信绝不自动发送**（只读、只出草稿；产品里不允许存在发送路径）；
- 数据只在本机处理，**界面必须如实说明谁来处理**（云端模型时要说清发给谁）。

## 2. 面向用户的东西有硬约束（有测试在挡，别绕过）

- 文案：**全中文、零术语**。黑名单词（模型/provider/token/会话/上下文/路径/终端/缓存…）由 `gui/src/simple/copy-guard.test.ts` 扫；内联文案有一份**只许减少**的预算表——把句子搬进 `copy*.ts`，**不要**去调大数字。
- 字号：正文 ≥16px、标题 ≥20px、按钮 ≥44px，由 `gui/src/simple/typography.test.ts` 扫。
- 平台：产品**以 Windows 为先**。`gui/src/simple/tasks/platform.test.ts` 会扫每张卡**在生产路径上真正发出的指令**，命中 `textutil`/`sips`/`/Users/`/`访达` 等平台专有写法就红。

## 3. 最容易犯、代价最大的三个错（都真的发生过）

1. **走了产品不会走的路径**。发出去的指令必须由 `instructionFor(taskId, files, instruction)` 组装（`store.composedInstruction` 用的就是它）。**不要**直接调 `task.prompt()` 做验证——那曾经让一个 P0（卡片提示词从未发出）潜伏很久，而"真机验收"看着一直是绿的。
2. **把"没验证"写成"通过"**。跑不了就说跑不了，并说清为什么。报告里宁可写"这一条我没验证（原因：…）"，也不要写"应该没问题"。
3. **测试红了就放松测试**。先判断是测试的问题还是产品的问题：产品错就修产品；测试假设过期就**加强**它（例如从"等值断言"改成"包含两组内容 + 长度下界"），并在提交信息里说清为什么。

## 3.5 怎么少烧上下文（实测：贵的是输入，不是输出）

我们用 `opencode stats` 量过自己的账：**输入 4.2M / 输出 114K / 缓存读 13.6M**（15 个会话、平均 1.2M tokens/会话）。
**也就是说：生成几乎不要钱，反复把上下文装载进去才是成本。** 四条能立刻省下来的做法：

1. **先 `grep` 再 `read`**：没定位就整份读大文件是最大的浪费（`gui/src/store.ts` 900 行、验收报告 20KB+）。
   用 `grep -n` 找到行号，再 `read` 那一小段。
2. **输出只贴尾巴**：`| tail -20`、`--quiet`、`Select-Object -Last 5`。**不要把整份测试输出、整份日志、整个 JSON
   灌回上下文**——那正是 4.2M 输入的主要来源。
3. **一个任务一个会话**：别在长会话里接着聊下一件事（平均 1.2M tokens/会话就是这么来的）。本地起的 agent
   用 `--print --no-session` ✓，每个任务从零开始。
4. **让缓存命中**：同一个会话里别改 system prompt / 配置（那会让前缀失效）；**稳定的内容放前面、变化的内容放后面**
   （我们的指令信封就是这么排的 ✓，13.6M 缓存读说明它在起作用 ✓）。
5. **别在这儿做 CI 能做的事**：构建、全量测试交给 CI（那边是免费并行 ✓）。在那台 Windows 上推分支前只跑
   `gui/scripts/check-windows.ps1`（**实测 5 秒**），而不是 `e2e.sh`（**30 分钟以上**）。

## 3.6 验收必须走**真实那条路**，不许验"我们的意图"

同一个错误我们犯过两次，两次都差点被当成"已验证" ✗：

1. **卡片提示词从未发出** ✗（见 §3.1）：`instructionFor()` 写好了、测试也过了，但**生产路径**从来没用它
   —— 真正发出去的只有用户那句话 ✓。修法：验收一律走 `store.composedInstruction(run)` ✓，
   **不许**用 `task.prompt()` ✗（那是产品不会走的路 ✓）。
2. **打包：配置 ≠ 产物** ✗：`tauri.conf.json` 里的声明看着对、静态测试也过了 —— 但**没有人打开过
   发布出去的 macOS dmg** ✗（三轮"真机验收"验的都是 Windows 安装包）✗。
   → 后来真打开了（`gui/scripts/verify-bundle.sh` ✓）：四个可执行文件都在 ✓、都能跑 ✓、没有垃圾 ✓。
   **但这条教训仍然成立，而且我自己又踩了一次** ✗：第一次读的是一个**已强制卸载过的挂载**，看到残缺
   内容就报了个"两个工具没进包"的 P0 ✗ —— 我确实"打开了产物"，但**读错了**，而且**只读了一次就下结论** ✗。
   **判据再加一句**：读到**可疑**结果时，先**重新挂载 / 重跑一次复核**再说话 ✗✗；能把那次检查写成脚本
   就写成脚本 ✓（`verify-bundle.sh` 现在每次发布都跑 ✓）。

**判据**：说"已经验证"之前，先问一句"**我看的，是它真的产出/真的执行的那个东西吗？**"

- 打包 → **打开产物看**（挂载 dmg、列安装目录 ✓），不看配置文件；
- 界面 → 走**用户真的会点的**那条路（真点、真读控件值 ✓），不读"看起来接好了"；
- 助手行为 → 看**真正发出去的那段文字** ✓。

## 4. 代码结构要点

- 任务卡：`gui/src/simple/tasks/*.ts`。共享类型在 `tasks/types.ts`、构建函数在 `tasks/prompt.ts`——**卡片模块只依赖这两个叶子模块**，不要从 `tasks/index.ts`（barrel）取类型，那会形成循环导入（曾让"单跑一张卡的测试"直接崩）。
- 新增卡片数组后，**由集成者把它接进 `tasks/index.ts` 的 `TASKS`**（你可以在报告里注明"待接线"）。`tasks/platform.test.ts` 里有一份模块登记表，加卡要顺手登记，否则它会红——失败信息会指名要登记哪个文件。
- 界面文案一律进 `copy*.ts`；纯逻辑（可单测、无 Solid 依赖）优先放进 `src/simple/*.ts`，组件只做渲染。
- Rust 侧：`gui/src-tauri/src/`（`daemon.rs` 协议与进程、`files.rs` 文件事实、`sheets.rs`/`pdf.rs` 自带工具、`admin_config.rs` 企业预置）。**编译警告在 CI 上等于错误**。

## 5. 在你自己那台机器上怎么自检

| 你能跑 | 命令 | 跑不了 |
| --- | --- | --- |
| 前端测试与类型 | `cd gui && bun test src && bunx tsc --noEmit` | — |
| 真机任务验收 | `bash gui/scripts/task-sweep.sh <卡片id>`（需要模型端点环境变量） | Windows 原生没有守护进程，见 §6 |
| Rust / 打包 | — | **交给 CI**（`cargo test`、双平台 job、安装包）。跑了才写，没跑别写 |

**超时要设够**：单卡/单轮至少 **1800 秒**（慢模型实测 850 秒；设 600 会把成功误判成失败）。网关并发别贪多——并发过高会被断连，每次重连多等 60 秒。

**但你自己跑的每条可能变慢的命令，必须带超时**——这条是真金白银换来的：一次远程会话里，
一条用 WindowsInstaller COM 查 MSI 表的脚本跑了 20 分钟、烧掉 967 秒 CPU，**把那个 agent 的
整轮堵死**（它在等一个永远不返回的命令，而外面看起来只是"没动静"）。

```powershell
# PowerShell：把活儿放进 Job 等，超时就停掉并如实报告"超时"
$j = Start-Job { ... }; if (Wait-Job $j -Timeout 60) { Receive-Job $j } else { Stop-Job $j; "超时：60 秒没跑完" }
```
```bash
# bash / python 同理
timeout 60 <命令>            # bash
result = subprocess.run(..., timeout=60)   # python
```

报告里 **"超时" 与 "确认没有" 必须分开写**——把"没等到结果"写成"没有"，是我们最怕的那种错。

### 5.5 在新 worktree 里跑测试，必须用 `e2e.sh`（它会 `bun install`）

我一天里犯了两次同一个错 ✗：在新 worktree 里直接 `bun test src` → 报一堆红，
**其实是依赖没装全** ✗（例如 `Cannot find module 'react/jsx-dev-runtime'`）——
差一点就把一个**其实是绿的**改动判成红的、或者反过来去"修"一个不存在的问题 ✗。

**规矩**：新 worktree 里的第一件事是 `bash gui/scripts/e2e.sh` ✓（它是 7 步，第 2 步就是 `bun install` ✓）。
要单跑某个测试文件，先跑过一次 `e2e.sh` ✓ 或 `cd gui && bun install` ✓。
**判"红"之前先问**：这是产品的问题，还是我没装东西 ✗？

## 6. Windows 的特殊事实（很重要）

- 上游 `cante`/`ante` **只有 macOS 与 Linux 构建**（官方 README：Windows 建议用 WSL）。所以**Windows 原生跑不了真实任务**：应用会去找 `cante`（`CANTE_BIN` 或 PATH），找不到就没有会话。
- 想在那台 Windows 上做**真实端到端**验收：在 **WSL** 里跑 Linux 版守护进程，然后把 `CANTE_BIN` 指过去（例如 `wsl.exe -e /home/<用户>/cante-bin/ante serve`）。WSL 里能访问局域网网关。
- **SSH 会话里看不到窗口**：从 SSH 启动的 GUI 进程没有交互桌面，截图拿不到真实画面。要看窗口、点按钮，需要 **RDP 会话**（或把任务挂到已登录会话上）。这条限制要在报告里如实写。

## 6.5 两个会让"推不上去"的坑（Windows 上尤其）

1. **改动 `.github/workflows/**` 的分支，可能推不上去**。那台 Windows 测试机上 git 用的是
   **凭据管理器里缓存的旧令牌**（缺 `workflow` 权限），于是 `git push` 报
   `refusing to allow an OAuth App to create or update workflow … without workflow scope` ✗。
   **遇到就如实写进报告**，别反复重试 ✗ —— 集成者会把这个提交取回本机来推 ✓
   （本机的凭据有权限 ✓）。
2. **在 Windows 上生成 patch/重定向输出，别用 `>`**。PowerShell 的重定向按**控制台编码**（GBK）
   写文件，中文会变成非法字节，`git am` / `git apply` 都会说"不是合法补丁" ✗。
   让 **git 自己写文件**：`git format-patch -1 HEAD --output=…` ✓（或 `git show > ` 换成
   `--output` 类的写法 ✓）。

### 6.9 验证在哪台机器上跑：看 `gui/VERIFICATION-MAP.md`

「已验证」必须能回答"**在哪台机器上、看的是什么产物**"✗✓。这张表把每条验证指到**可执行的脚本**或
**具体的记录文件**，并写明它**不覆盖**什么 ✗（例如本机 `dom-smoke` 不覆盖**显示缩放**、也不覆盖真 WebView2 ✓）。
新增验证脚本时**顺手补一行**。

## 7. 交付方式

- 一个任务一个分支（`ws/<轮次>-<名字>` 或 `fix/<简述>`），**只改任务范围内列出的文件**；不要顺手重构无关代码。
- 提交信息：讲**为什么**，不要用反引号（会打断某些 shell）。
- 收尾必须给：**改了什么 / 测试结果 / 真机结果（含失败与不确定）/ 没做的事及原因**。
- 能开 PR 就开 PR（`gh pr create`）；开不了就把分支推上去并在报告里说明。**不要自己合并**——合并由集成者做（本机只有一个身份，GitHub 拒绝自批，所以 review 通过 CI 双平台 + 真机证据体现）。

### 7.5 别用"手工合并分支"代替合并 PR

我做过一次：把几个分支**手工并进一个集成分支**再开一个 PR 合掉 ✓ —— 结果那**五个原来的 PR 一直开着** ✗，
过了很久才发现其中 **两个还有真内容没进 main** ✗（Windows 快检脚本、AGENTS.md 的一节）—— 差一点就丢了 ✓。

**规矩**：合并一律走 **PR** ✓；确实需要手工并（例如为了先解冲突 ✓）时，**并完立刻把那几个 PR 关掉并说明原因** ✓。
判断"这个 PR 还有没有货"的方法**只有一个**（别的都会骗你 ✗）：把它合进临时分支，
看 `git diff --cached` 相对 main **是不是空的** ✓。

## 8. 当前优先级（2026-09，供参考，以 issue 为准）

1. Windows 真机验收（安装包、SmartScreen、黑窗、中文路径、以及"没有守护进程"这件事怎么向用户交代）；
2. `#48` 照片/截图转表格（受宿主是否真的把图片送出去限制，见 ROADMAP 的诚实边界）；
3. 结果交付（统一结果位置、复制成微信能贴的文字）；
4. 企业预置（让公司敢批：默认网关、只许本机、禁用某些卡）。
