---
title: "网关模式"
sidebar_label: "网关模式"
description: "将 Cante 作为长寿命机器人接入 Slack 或 Discord"
---

# 网关模式

网关模式（Gateway mode）允许将 Cante 作为长寿命机器人运行，可从 Slack、Discord 或同时从两者接入访问。每一次对话 —— 频道中的讨论串（thread）或私聊（DM）—— 都会分配到独立的智能体会话，使得多用户和多团队可以并发与 Cante 协作而互不干扰状态。

## 基础用法 {#basic-usage}

```bash
# 首先启动 Cante 宿主服务
cante serve --sock

# 在另一个终端中启动网关
cante gateway
```

`cante gateway` 通过 Cante SDK 连接到正在运行的 Cante 宿主（`cante serve --sock` 或通过 `--connect`），而非在进程内直接管理运行时。它会读取 `~/.cante/channels.json`，连接您启用的所有平台，并开始监听消息。

### 从源码运行独立网关 {#standalone-gateway-from-source}

同款渠道配置的独立 SDK 客户端 `ante-gateway` 位于上游仓库 [AntigmaLabs/ante](https://github.com/AntigmaLabs/ante) —— 它**不在本仓库中**，而且**尚未发布**：负责发布它的工作流目前仍需手动触发，因此今天没有可安装的包或下载。下面描述的是那个 crate，只有在你已经检出该仓库之后才适用：

```bash
cargo install --path ante-gateway
ante-gateway
```

请先按上文启动 `cante serve --sock`，或者使用 `ante-gateway --connect stdio` 为每个对话拉起一个 Cante 宿主。使用 stdio 时，可执行文件依次从非空的 `CANTE` 环境变量、`--executable PATH`、`PATH` 上的 `cante` 解析。该独立客户端基于 SDK/协议 0.2.1 构建，请配合兼容的 Cante 宿主使用。

独立可执行文件支持下面列出的全部网关参数，另加用于 stdio 连接的 `--executable`。`CANTE_HOME` 可覆盖 `channels.json` 与 `run/serve.sock` 所用的默认 `~/.cante` 目录；`CANTE_SERVE_TOKEN` 为 WebSocket 宿主提供 Bearer Token。日志输出到 stderr，而 `--output-format` 控制 stdout 上的协议事件。

安装该 crate 不会替换上文记录的 `cante gateway` 命令：独立客户端是需要你直接调用的另一个可执行文件。

## 配置文件 {#configuration}

创建 `~/.cante/channels.json`。您可以仅启用 Slack、仅启用 Discord 或两者同时启用。

```json
{
  "slack": {
    "enabled": true,
    "bot_token": "env:SLACK_BOT_TOKEN",
    "app_token": "env:SLACK_APP_TOKEN",
    "allow_from": "open"
  },
  "discord": {
    "enabled": true,
    "bot_token": "env:DISCORD_BOT_TOKEN",
    "allow_from": ["123456789012345678"]
  }
}
```

以 `env:` 开头的值会在启动时从环境变量中读取，避免将敏感 Token 明文保存在文件中。

### 访问控制 {#access-control}

`allow_from` 字段决定机器人会响应谁的消息：

| 取值 | 允许与机器人对话的受众 |
| --- | --- |
| `"open"` | 工作区或服务器内的所有成员 |
| `["U123", "U456"]` | 仅限列表中的具体用户 ID |
| *(省略)* | 拒绝所有访问（安全的默认策略） |

## 配置 Slack {#setting-up-slack}

1. 访问 [api.slack.com/apps](https://api.slack.com/apps) → **From scratch** 创建应用。
2. **Settings → Socket Mode**：开启 Socket Mode 并创建一个拥有 `connections:write` 权限的 App-Level Token。复制 `xapp-…` Token —— 作为 `app_token`。
3. **Features → OAuth & Permissions → Bot Token Scopes**：添加 `chat:write`、`app_mentions:read`、`channels:history`、`im:history`、`im:write` 权限。
4. **Features → Event Subscriptions**：开启事件订阅并订阅 `message.channels` 和 `message.im`。
5. **Features → App Home**：启用 Messages Tab 并允许用户直接发送消息。
6. **Settings → OAuth & Permissions → Install to Workspace**。复制生成的 `xoxb-…` Bot User OAuth Token —— 作为 `bot_token`。
7. 在每个需要机器人的频道中运行：`/invite @your-bot-name`。

```bash
export SLACK_BOT_TOKEN="xoxb-..."
export SLACK_APP_TOKEN="xapp-..."
```

**在 Slack 中使用机器人：** 在频道中 `@提及` 机器人，或直接发送私聊 DM。机器人的回复始终汇总在讨论串（thread）中。

:::note
任何时候修改了权限（Scopes）或事件订阅后，必须点击 **Reinstall to Workspace** 重新安装才能生效。
:::

## 配置 Discord {#setting-up-discord}

1. 访问 [discord.com/developers/applications](https://discord.com/developers/applications) → **New Application** 创建应用。
2. **Bot**：复制 Token —— 作为 `bot_token`。在 **Privileged Gateway Intents** 下开启 **Message Content Intent**（必选）。
3. **OAuth2 → URL Generator**：在 **Scopes** 中勾选 `bot`；在 **Bot Permissions** 中勾选 `Send Messages`、`Read Message History`、`View Channels`。打开生成的 URL 将机器人邀请到您的服务器。

```bash
export DISCORD_BOT_TOKEN="..."
```

**在 Discord 中使用机器人：** 在服务器频道中 `@提及` 机器人，或发送私聊 DM。机器人会通过引用回复你的消息 —— 直接回复机器人的消息即可继续对话（会话讨论串中的后续回复会自动路由回原对话，无需重复显式 @提及）。

## 与机器人交互 {#talking-to-the-bot}

当机器人加入工作区或服务器且网关正在运行后，您可以像与普通机器人一样与其交谈。部分特定回复具有特殊含义。

您可以随时通过发送以下指令中断机器人：

| 指令 | 效果 |
| --- | --- |
| `stop` | 取消当前任务（类似于 TUI 中的 Ctrl+C） |

当机器人请求执行工具的权限时，回复以下内容之一：

| 回复 | 效果 |
| --- | --- |
| `yes`（或 `y`） | 批准本次工具调用 |
| `no`（或 `n`） | 拒绝本次工具调用 |
| `always` | 批准并在当前会话的后续调用中始终允许该工具 |

更多关于工具审批流程的说明，请参见[授权与审批](/usage/approvals)。

## 命令行参数参考 {#cli-reference}

```bash
cante gateway [--config PATH] [--connect ENDPOINT] [--model MODEL] [--provider PROVIDER] [--output-format FORMAT]
```

| 参数标识 | 默认值 | 说明 |
| --- | --- | --- |
| `--config` | `~/.cante/channels.json` | 渠道配置文件的路径 |
| `--connect` | `unix:~/.cante/run/serve.sock` | Cante 宿主端点：`unix:<path>`、`ws://<addr>`、`wss://<addr>` 或 `stdio`。可通过 `cante serve --sock` 启动宿主 |
| `--model` | — | 请求的模型（未指定时使用宿主设置） |
| `--provider` | — | 请求的提供商（未指定时由宿主解析） |
| `--output-format` | `minimal` | 网关 stdout 输出格式：`minimal`、`human` 或 `json` |

:::tip
默认情况下，`cante gateway` 连接本地宿主套接字 `unix:~/.cante/run/serve.sock`。若需连接其他端点，请传入 `--connect`（例如 `cante gateway --connect unix:/tmp/cante.sock`）。
:::

### 输出格式 {#output-formats}

| 格式 | 显示内容 |
| --- | --- |
| `minimal` | 仅输出智能体回复消息、通知信息与错误 |
| `human` | 彩色格式化输出所有事件（适用于调试） |
| `json` | 逐行输出 JSON Lines 格式的所有事件（适用于通过管道传递给其他工具） |

## 常见问题排查 {#troubleshooting}

**Slack 提示："Sending messages to this app has been turned off"**
在 **App Home** 下启用 Messages Tab 并允许用户发送消息，随后重新点击 Reinstall to Workspace。

**Slack 中机器人在频道被 `@提及` 时无响应**
确保机器人已被邀请加入该频道（`/invite @bot-name`），且 `message.channels` 已添加在 Event Subscriptions 中。修改事件或权限后请重新安装应用。

**Discord 中机器人无响应**
确认机器人已开启 **Message Content Intent**，且在频道中拥有 `Send Messages` 和 `Read Message History` 权限。

**网关收到消息后未输出任何内容**
使用 `--output-format human` 运行以查看所有事件，并检查 `~/.cante/logs/` 中的详细错误信息。
