---
title: 快速入门
description: "在一分钟内安装 Cante 并开始使用"
---

## 前置准备 {#prerequisites}

- 至少一家大语言模型提供商（Anthropic、OpenAI 等）的 API 密钥或订阅 —— 或者使用[离线模式](/local/offline)无需任何 API 密钥

## 安装 {#installation}

Cante 作为单一独立的二进制文件分发，没有任何外部运行时依赖 —— 下载即可直接运行。

```sh
curl -fsSL https://cante.run/install.sh | bash
```

默认情况下会安装至 `~/.cante/bin`。若想安装到其他目录，可设置 `CANTE_INSTALL_DIR` 环境变量：

```sh
curl -fsSL https://cante.run/install.sh | CANTE_INSTALL_DIR=/usr/local/bin bash
```

请以普通用户身份运行默认安装脚本，不要使用 `sudo`。若使用 sudo 安装到用户主目录会导致 `~/.cante` 归属于 root 用户，从而阻止 Cante 正常保存设置和输入历史，因此安装脚本会拒绝该组合。

macOS 平台的发布二进制文件已经过 Apple 开发者证书签名与公证（Notarized），Gatekeeper 安全检查会自动放行。

## 快速示例 {#quick-examples}

### 交互式会话 {#interactive-session}

```bash
# 启动 TUI — 与智能体对话、批准工具调用、查看 diff
cante
```

### 单次执行（无头模式） {#headless-one-shot}

```bash
# 运行单个任务并退出
cante -p "add error handling to src/main.rs"
```

### 从标准输入通过管道传递内容 {#pipe-input-from-stdin}

```bash
# 传递文件内容进行分析
cat src/lib.rs | cante -p "review this code for bugs"
```

### 切换模型提供商 {#use-a-different-provider}

```bash
# 覆盖模型和提供商
cante --provider openai --model gpt-5.6-sol -p "refactor this function"
```

### 跳过工具审批 {#skip-tool-approvals}

```bash
# YOLO 模式 — 自动批准所有工具调用
cante --yolo -p "fix all clippy warnings"
```

### 作为服务端运行 {#run-as-a-server}

```bash
# 启动长寿命守护进程以进行编程集成
cante serve
```

### 更新到最新版本 {#update-to-the-latest-version}

```bash
# 检查新版本并安装
cante update

# 尝试开发通道单次更新
cante update nightly

# 回滚或锁定到指定发布版本
cante update --version v0.2.1
```

## 下一步 {#whats-next}

<CardGroup cols={2}>
  <Card title="常用工作流技巧" icon="book-marked" href="/usage/providing-context">
    涵盖模型、文件、工具授权等常见 TUI 交互指南。
  </Card>
  <Card title="TUI 终端界面指南" icon="terminal" href="/usage/tui">
    熟练掌握交互式终端界面的全部操作。
  </Card>
  <Card title="无头模式" icon="code" href="/usage/headless">
    了解 CLI 命令行参数与输出格式。
  </Card>
  <Card title="本地模型" icon="wifi-slash" href="/local/overview">
    在自有硬件上直接运行模型。
  </Card>
  <Card title="服务模式" icon="server" href="/usage/serve">
    通过 JSONL 协议以编程方式驱动 Cante。
  </Card>
  <Card title="Skills 技能系统" icon="scroll" href="/extend/skills">
    通过轻量便携的 Agent Skills 扩展 Cante 的专业能力。
  </Card>
</CardGroup>
