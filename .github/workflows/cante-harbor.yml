name: Harbor Test

on:
  workflow_dispatch:
    inputs:
      install_args:
        description: 'install.sh args for the Cante build to test (e.g. stable, nightly, a version); empty = install.sh default'
        required: false
        default: ''
      harbor_args:
        description: 'Arguments to pass to harbor'
        required: true
        default: '--dataset terminal-bench@2.0 --n-attempts 5'
      model_name:
        description: 'Model name to use'
        required: false
        default: 'gemini-3-pro-preview'
      provider:
        description: 'Model provider (anthropic, openai, gemini, etc.)'
        required: false
        default: 'gemini'
      effort:
        description: 'Reasoning effort passed to cante --effort'
        required: false
        default: ''
      model_base_url:
        description: 'Base URL for model API'
        required: false
        default: 'https://generativelanguage.googleapis.com/v1beta'
      model_temperature:
        description: 'Model temperature'
        required: false
        default: ''
      model_max_tokens:
        description: 'Max tokens for model'
        required: false
        default: ''
      runs_on:
        description: 'Runner to use for the test job'
        required: false
        default: 'gcp-hosted'
      run_count:
        description: 'Number of times to run Harbor tests (each run is a separate job with 6h limit)'
        required: false
        default: '1'

jobs:
  # First job: Generate matrix based on run_count
  generate-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Display all workflow inputs
        env:
          INSTALL_ARGS: ${{ github.event.inputs.install_args }}
          HARBOR_ARGS: ${{ github.event.inputs.harbor_args }}
          MODEL_NAME: ${{ github.event.inputs.model_name }}
          PROVIDER: ${{ github.event.inputs.provider }}
          EFFORT: ${{ github.event.inputs.effort }}
          MODEL_BASE_URL: ${{ github.event.inputs.model_base_url }}
          MODEL_TEMPERATURE: ${{ github.event.inputs.model_temperature }}
          MODEL_MAX_TOKENS: ${{ github.event.inputs.model_max_tokens }}
          RUNS_ON: ${{ github.event.inputs.runs_on }}
          RUN_COUNT: ${{ github.event.inputs.run_count }}
        run: |
          echo "=========================================="
          echo "         Workflow Input Parameters        "
          echo "=========================================="
          printf 'install_args:      %s\n' "$INSTALL_ARGS"
          printf 'harbor_args:       %s\n' "$HARBOR_ARGS"
          printf 'model_name:        %s\n' "$MODEL_NAME"
          printf 'provider:          %s\n' "$PROVIDER"
          printf 'effort:            %s\n' "$EFFORT"
          printf 'model_base_url:    %s\n' "$MODEL_BASE_URL"
          printf 'model_temperature: %s\n' "$MODEL_TEMPERATURE"
          printf 'model_max_tokens:  %s\n' "$MODEL_MAX_TOKENS"
          printf 'runs_on:           %s\n' "$RUNS_ON"
          printf 'run_count:         %s\n' "$RUN_COUNT"
          echo "=========================================="

      - name: Generate run matrix
        id: set-matrix
        env:
          RUN_COUNT: ${{ github.event.inputs.run_count || '1' }}
        run: |
          case "$RUN_COUNT" in
            [1-9]|[1-9][0-9]|100) ;;
            *) echo "run_count must be an integer from 1 to 100" >&2; exit 1 ;;
          esac
          MATRIX=$(seq 1 "$RUN_COUNT" | jq -R . | jq -s -c .)
          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"
          echo "Generated matrix: $MATRIX"

  # Second job: Run Harbor tests (each matrix item is a separate job)
  test-cante-harbor:
    name: Test Cante Harbor (Run ${{ matrix.run_index }})
    needs: generate-matrix
    runs-on: ${{ github.event.inputs.runs_on || 'ubuntu-22.04-16core' }}
    timeout-minutes: 7200
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        run_index: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Clean up previous harbor (self-hosted only)
        run: |
          rm -rf $HOME/harbor || true

      - name: Install Docker if not present
        run: |
          if ! command -v docker &> /dev/null; then
            echo "Docker not found, installing..."
            curl -fsSL https://get.docker.com | sudo sh
            sudo usermod -aG docker $USER
            echo "Docker installed successfully"
          else
            echo "Docker already installed"
          fi
          # Ensure current user is in docker group
          if ! groups | grep -q '\bdocker\b'; then
            sudo usermod -aG docker $USER
          fi

      - name: Clone Harbor
        run: |
          cd $HOME
          git clone --depth 1 https://github.com/laude-institute/harbor.git

      - name: Run Harbor tests
        id: run-tests
        env:
          INSTALL_ARGS: ${{ github.event.inputs.install_args }}
          HARBOR_ARGS: ${{ github.event.inputs.harbor_args }}
          MODEL_NAME: ${{ github.event.inputs.model_name }}
          PROVIDER: ${{ github.event.inputs.provider }}
          EFFORT: ${{ github.event.inputs.effort }}
          MODEL_BASE_URL: ${{ github.event.inputs.model_base_url }}
          MODEL_TEMPERATURE: ${{ github.event.inputs.model_temperature }}
          MODEL_MAX_TOKENS: ${{ github.event.inputs.model_max_tokens }}
          RUN_COUNT: ${{ github.event.inputs.run_count }}
          RUN_INDEX: ${{ matrix.run_index }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CANTE_ENV: github_action
        run: |
          cd $HOME/harbor

          echo "=== Run $RUN_INDEX of $RUN_COUNT ==="
          echo "Running harbor with args: $HARBOR_ARGS and model: $MODEL_NAME"

          # Only the selected provider's credential is forwarded into the sandbox.
          case "$PROVIDER" in
            anthropic) CRED=ANTHROPIC_API_KEY ;;
            openai) CRED=OPENAI_API_KEY ;;
            gemini) CRED=GEMINI_API_KEY ;;
            *) echo "Unsupported provider: $PROVIDER" >&2; exit 1 ;;
          esac
          if [ -z "${!CRED}" ]; then
            echo "$CRED is not set for provider $PROVIDER" >&2
            exit 1
          fi

          CMD=(uv run harbor run
            --agent cante_agent:CanteAgent
            --model "$MODEL_NAME"
            --ak "provider=$PROVIDER"
            --ak "install_args=$INSTALL_ARGS")
          if [ -n "$EFFORT" ]; then
            CMD+=(--ak "reasoning_effort=$EFFORT")
          fi
          # ${KEY} is passed literally; harbor expands it when building the sandbox env.
          CMD+=(--ae "$CRED=\${$CRED}")
          for key in MODEL_BASE_URL MODEL_TEMPERATURE MODEL_MAX_TOKENS CANTE_ENV; do
            if [ -n "${!key}" ]; then
              CMD+=(--ae "$key=\${$key}")
            fi
          done
          read -r -a HARBOR_ARGV <<< "$HARBOR_ARGS"
          CMD+=("${HARBOR_ARGV[@]}")

          # The adapter is imported from the checkout; harbor resolves it via PYTHONPATH.
          export PYTHONPATH="$GITHUB_WORKSPACE/cante-harbor${PYTHONPATH:+:$PYTHONPATH}"

          # Use sg to run with docker group permissions (avoids needing to log out/in)
          sg docker -c "$(printf '%q ' "${CMD[@]}")"

      - name: Collect results
        if: always()
        run: |
          cd $HOME/harbor
          mkdir -p ${{ github.workspace }}/test-results
          cp -r jobs/* ${{ github.workspace }}/test-results/ || true
          cp -r trials/* ${{ github.workspace }}/test-results/ || true

      - name: Upload test results as artifact
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: cante-harbor-results-${{ github.run_id }}-run-${{ matrix.run_index }}
          path: test-results/
          retention-days: 30
          if-no-files-found: warn
