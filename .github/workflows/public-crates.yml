name: Public Crates

on:
  pull_request:
    paths:
      - 'crates/**'
      - 'examples/**'
      - 'cante-acp/**'
      - 'rustfmt.toml'
      - '.github/workflows/public-crates.yml'
  push:
    branches: [main]
    paths:
      - 'crates/**'
      - 'examples/**'
      - 'cante-acp/**'
      - 'rustfmt.toml'
      - '.github/workflows/public-crates.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Check ${{ matrix.crate }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - crate: protocol-shape
            manifest: crates/protocol-shape/Cargo.toml
            workspace: crates/protocol-shape
          - crate: exec
            manifest: crates/exec/Cargo.toml
            workspace: crates/exec
          - crate: cante-sdk
            manifest: crates/cante-sdk/Cargo.toml
            workspace: crates/cante-sdk
          - crate: llm
            manifest: crates/llm/Cargo.toml
            workspace: crates/llm
          - crate: cante-acp
            manifest: cante-acp/Cargo.toml
            workspace: cante-acp
          - crate: mini-tui
            manifest: examples/mini-tui/Cargo.toml
            workspace: examples/mini-tui

    steps:
      - uses: actions/checkout@v7
      - uses: actions-rust-lang/setup-rust-toolchain@v1.17.0
        with:
          toolchain: stable
          cache-workspaces: ${{ matrix.workspace }} -> target
      - name: Build
        env:
          MANIFEST: ${{ matrix.manifest }}
        run: cargo build --manifest-path "$MANIFEST" --all-targets
      - name: Run tests
        env:
          MANIFEST: ${{ matrix.manifest }}
        run: cargo test --manifest-path "$MANIFEST"
