# 第 16 批机会点：**服务方自己也会有事**——今天它真的欠费停机了

起点：第 15 批（#278–#283）全部合并 ✓，0 开着 ✓。
这一批的**头号机会点不是推理出来的**：派活的网关 `commandcode` 通道**今天欠费停机** ✗，
把我两个在跑的 agent 打断在半途 ✗（错误原文：
`503: {"message":"...[400]: You have insufficient credits to make this request..."}` ✓）。
**我们的 agent 遇到的，正是王姐会遇到的。**

---

## P1 · 服务方"没钱了/太忙了"时，界面认不出它 ✗（**新**，一手现场）

**判据（核过 ✓）**：
1. 出错出路由 `gui/src/simple/recovery.ts` 按**错误特征**分流 ✓——
   `NETWORK`（断网 ✓）、`AUTH`（密钥没配好 ✓，认 `401/403/api key/unauthorized` ✓）、
   `FORMAT`、`UNSURE`、停止 ✓ 等，每类有各自的中文出口 ✓；
2. 但**没有一类认得**这些真实现场 ✗：
   `insufficient credits`、`quota exceeded`、`rate limit`、`429`、`503`、`余额不足`、
   `用量用完了`、`reset after 15s`——**全部落到兜底那句**（"出了点问题"类 ✗）；
   更坏的是部分文案会引向「重新选一次文件」——**服务方没钱和她的文件毫无关系** ✗。
   （`AUTH` 会把 `402/403` 一类的数字误抓吗 ✓——要核 `40[13]` 与 429/503 的边界 ✓。）
3. **她的处境**：公司网关这个月用量用完 / 服务方抽风 5 分钟。
   她要的答案是「**等一会儿再试**，这事**多半不归你管**，一直这样就去问管网络的同事」✓；
   她**不该**得到的答案是"你的文件有问题"✗ 或"你没配好"✗（那是 AUTH 的说法 ✓）。
4. 这也是验收地图里挂着的那条**「断网/服务方不可用时的表现」没验** ✓ 的一半 ✓
   （另一半"真拔网线"仍要真机 ✓）。

**要做**：先核现有识别面与互相抢的特征（别把"断网"和"没额度"混成一类 ✓——出路不同 ✓）；
新增一类「**服务方暂时忙/用完了**」：中文、说清**这不是她的错** ✓、给"稍后再试 + 找谁"✓；
夹具（`fake-cante`）要能演这一出 ✓（不然回归挡不住 ✓）；全中文零术语 ✓。

## P1 · Windows 真机验收**这一批的修复**在产物里真的在 ✓（第 15 批的收尾）

第 15 批修了三处、**都没真机验过** ✗（各自报告都如实标了 ✓）：
① GBK 的 CSV → 中文报错 +「另存为」那一步（#279 ✓）；
② 坏 xlsx → 她看中文、原文进详情（#282 ✓）；
③「复制位置」按钮 + 反斜杠完整位置（#283 ✓）。
按 #247 的教训：**先看 exe 时间戳、再 grep 这次新加的文案**，别再量旧构建 ✗。

## P2 · 补上 #282 该交的**英文泄漏地点清单**（收尾欠账）

`readleak` 那轮被欠费打断 ✗：A1 的清单（库里/系统原话**所有**能到她面前的地方）
只留在会话日志里 ✗ 没落成文档 ✗。要有人**重新走一遍**（`sheets.rs`/`pdf.rs`/`files.rs`/
`commands.rs` 里所有 `Display` 拼库原文、`from_utf8_lossy` 端 stderr 的地方 ✓），
逐条标「现在还会到她面前吗 / 已被哪一层挡住 / 依据」✓ → `gui/docs/SHEETS-ERROR-TEXT.md`。

## 已核：**不是缺口** ✗（省得下轮再查）

| 怀疑 | 核过的事实 | 结论 |
| --- | --- | --- |
| 断网/超时没出路 | `copy.ts` `offlineHint`/`networkWhat`/`stallWhat`（#173 停滞屏 ✓ 还带「做到哪儿了」✓）+ `recovery.ts` NETWORK 认 `ECONNREFUSED` 一族 ✓ | **已有** ✓ |
| 任务被停下 | `stoppedWhat` ✓ + 取消类正则 ✓ | **已有** ✓ |
| 密钥没配好 | `AUTH` 认 `api key/401/403/unauthorized/密钥/未授权` ✓ | **已有** ✓（本轮别和它混类 ✓）|
| 磁盘写不下 | `spaceWhat` + 出路 ✓ | **已有** ✓ |
| 结果名没校验 | #279 已加 `BadOutputName` ✓（"假 .csv"修掉 ✓）| **已有** ✓ |

---

## 边界（不做 ✗）

不做"自动重试/自动换服务方"（她不该看到这些概念 ✗，重试由助手侧做 ✓）；
不碰红线（复制≠发送 ✓）；不为通过放松闸门（copy/typography/tone/platform/a11y/秘密扫描 ✓）；
不修 `files.rs` 那处正斜杠 `display()`（#283 已论证不值 ✗）。
