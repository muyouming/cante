# Windows 验收 28：键盘点「复制位置」一次成功（Tab 路线 + 哨兵剪贴板）

日期：2026-09-21　机器：那台 Windows 11 验收机（真 WebView2 + 真剪贴板）
逐字输出全文：`C:\Users\<用户名>\r18-panel.txt`（本报告只抄关键几段，其余以那份为准）。

---

## 0. 一句话结论

**通过** ✓：「我做的结果」面板**每行三个按钮**（打开 / 打开所在文件夹 / 复制位置 ✓），
用**键盘** Tab 走到第一行的「复制位置」、按 Enter，`Get-Clipboard` 拿到的是**那一行自己的完整路径**
（首=盘符 ✓ 尾=`.xlsx` ✓ 反斜杠 ✓ 不含正斜杠 ✓），界面逐字出现「已经复制好了……」。

---

## 1. 新 exe 的时间戳（先核，再往下走 ✓）

```
文件：C:\cante\gui\src-tauri\target\release\cante-gui.exe
LastWriteTime：2026-09-21 05:18:44
大小：10405376
sha256：2AC80A8E6CB96BB2431DD6E522CC2903F03EF9C28EF376C914101E334B5BEEB5
CANTE_BIN=C:\Users\<用户名>\AppData\Local\Microsoft\WinGet\Links\bun.exe C:\cante\gui\fixtures\fake-cante.ts
FAKE_CANTE_SEED=1
```

（这份 exe 就是含 #290 的产物；前端标记 `CANTE-BUILD|2026-09-21 05:18|0.2.3` 与它同一时刻 ✓。）

---

## 2. 三行真结果（夹具，跑完已还原 ✓）

`seed-result-rows.ps1 -Mode seed -Rows 3 -WorkDir C:\Users\<用户名>\r18-seed` → 3 份真 xlsx + 3 条 `runs.json` ✓。
跑完 `-Mode restore` → 与备份逐字相同 ✓、`seedrun` 计数 = 0 ✓、marker 已删 ✓。

---

## 3. 面板里每行的按钮（逐字 dump，读屏名带文件名 ✓）

面板段（`r18-panel.txt` §2 的 [49]–[59] 行）：

```
  [49] 回到首页   [98x44]
  [50] 一次复制成微信能贴的文字   [236x49]
  [51] 打开 结果_挑出华东区-1.xlsx   [104x49]
  [52] 打开 结果_挑出华东区-1.xlsx 所在的文件夹   [154x49]
  [53] 复制 结果_挑出华东区-1.xlsx 的位置   [106x49]
  [54] 打开 结果_挑出华东区-2.xlsx   [104x49]
  [55] 打开 结果_挑出华东区-2.xlsx 所在的文件夹   [154x49]
  [56] 复制 结果_挑出华东区-2.xlsx 的位置   [106x49]
  [57] 打开 结果_挑出华东区-3.xlsx   [104x49]
  [58] 打开 结果_挑出华东区-3.xlsx 所在的文件夹   [154x49]
  [59] 复制 结果_挑出华东区-3.xlsx 的位置   [106x49]
```

每行三个（按文件名归拢，§3）：

```
  第 1 行（结果_挑出华东区-1.xlsx）：3 个按钮
      打开 结果_挑出华东区-1.xlsx
      打开 结果_挑出华东区-1.xlsx 所在的文件夹
      复制 结果_挑出华东区-1.xlsx 的位置
  第 2 行（结果_挑出华东区-2.xlsx）：3 个按钮
      打开 结果_挑出华东区-2.xlsx
      打开 结果_挑出华东区-2.xlsx 所在的文件夹
      复制 结果_挑出华东区-2.xlsx 的位置
  第 3 行（结果_挑出华东区-3.xlsx）：3 个按钮
      打开 结果_挑出华东区-3.xlsx
      打开 结果_挑出华东区-3.xlsx 所在的文件夹
      复制 结果_挑出华东区-3.xlsx 的位置
```

读屏名都带文件名 ✓，高 **49px** ✓（≥44）。

---

## 4. 键盘 Tab+Enter 点「复制位置」（本轮的关键证据）

目标是第一行那个（读屏名 `复制 结果_挑出华东区-1.xlsx 的位置`）。从面板打开时的初始焦点起按 Tab，
每按一次记录**当前真实键盘焦点**（UIA `FocusedElement`）：

```
目标按钮（读屏名）：复制 结果_挑出华东区-1.xlsx 的位置
已把剪贴板置为哨兵：R18-KEYBOARD-SENTINEL
窗口拿到前台：True
  [1] 当前键盘焦点：Edit | 搜索做过的结果文件
  [2] 当前键盘焦点：Button | 一次复制成微信能贴的文字
  [3] 当前键盘焦点：Button | 打开 结果_挑出华东区-1.xlsx
  [4] 当前键盘焦点：Button | 打开 结果_挑出华东区-1.xlsx 所在的文件夹
  [5] 当前键盘焦点：Button | 复制 结果_挑出华东区-1.xlsx 的位置
Tab 走到目标按钮：True
按 Enter（真键盘事件）……
```

→ **Tab 顺序真的是她手能走的那条**（搜索框 → 一起复制 → 打开 → 打开文件夹 → 复制位置）✓。

---

## 5. `Get-Clipboard` 逐字

```
剪贴板（逐字）：C:\Users\<用户名>\r18-seed\seed\结果_挑出华东区-1.xlsx
长度（字符）：44
首字符：'C'   尾字符：'x'
以盘符开头：True
以 .xlsx 收尾：True
含反斜杠：True
含正斜杠：False
含本行文件名：True
不等于哨兵：True
```

（哨兵是 `R18-KEYBOARD-SENTINEL` —— 复制前后**真的变了** ✓，不是"本来就在"。）

---

## 6. 成功提示逐字

```
  Text | 已经复制好了。粘到微信里，或者「打开文件」的窗口里，都能用。
出现「已经复制好了」：True
出现「没能复制上」：False
面板仍开着：True
```

---

## 7. 结论（只指向上面贴出来的输出）

1. 每行三个按钮都在、名字都带文件名 ✓（§3）；
2. **键盘** Tab 能走到「复制位置」并**按 Enter 触发** ✓（§4）；
3. 剪贴板是那一行自己的**完整路径**：盘符开头、`.xlsx` 收尾、反斜杠、无正斜杠 ✓（§5）；
4. 界面逐字出现「已经复制好了……」、没有失败句、面板不关 ✓（§6）。

**没有**"应该没问题"：上面每条都对着逐字输出 ✓。

---

## 8. 没验到的（分开写）

- **第 2、3 行的键盘点击**：本轮键盘只点了第一行（那是任务要求的"点一个"）✓；三行"各拿各的"
  由上一轮验收 26 的 UIA/真鼠标覆盖，**本轮没有**用键盘逐行点 ✗。
- **真鼠标点击**：本轮没做（上一轮验收 26 做过并通过 ✓）。
- **界面那句「这次没能核对它还在不在」**：是夹具（没有真 bridge）下 `fetchFileFacts` 的回退，
  本轮**没有**验"文件在/不在"的真实分支 ✗。
- **显示缩放 ≠ 100%**、**装机版那份 `cante-gui.exe`**、**粘到微信之后的观感**：**都没验** ✗。

---

## 9. 一句对上一轮 §7 的更正

上一轮（验收 26 §7）我写过"合成 Enter 在 WebView2 上不可靠"——本轮用 **Tab 走到按钮再 Enter**
（而不是 `SetFocus` + `keybd_event`）时，**一次就成功** ✓。
所以那条限制是 **`SetFocus` 没能把 DOM 焦点搬到那个按钮**造成的，**不是**产品键盘路径的问题 ✓。
（真键盘这条路本轮已验 ✓。）
