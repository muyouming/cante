# 第二十三次真机验收：结果面板的标题结构 —— **这一轮量的是旧构建，结论作废** ✗

**机器**：Windows 11 Home（win11 测试机）✓　**真 WebView2** ✓
**探针**：`gui\scripts\windows\a11y\probe-result-headings.ps1` ✓（进仓库 ✓）

---

## 0. 结论先行：**这一轮不能说明任何事** ✗（先说清为什么）

量出来的数字是：

```
List 个数 = 1 ; ListItem 个数 = 12 ; 带标题的候选 = 9
每份结果第一个标题后代 = 0 / 总结果数 = 12   → 有结果没有标题 ✗
List name=[] ✗ 不是「一共 N 份结果」；列表名字合规 = 0 / 1
确认页标题数 = 6
```

**看起来像"#239 没生效"** ✗ —— **但不是** ✓。我核了**被测的那个 exe** ✓：

```
装的 exe 时间 = 2026-09-20 02:27:50
大小 = 14936064
含「一共」= False
含「份结果」= False      ← #239 加的文案（`一共 N 份结果`）不在这个 exe 里 ✗
```

而 **`#239` 是 02:27 之后才合并进 main 的** ✓。→ **这一轮量的是旧构建** ✗ ✓
—— **它证明不了 #239 有没有效** ✗，也**证明不了**面板里现在有没有标题 ✗。

**这正好又是 `AGENTS.md` §3.6 那条** ✓：“**我看的，是它真的产出/真的执行的那个东西吗？**”
—— **没有** ✗（我看的是一个**比改动还老**的产物 ✓）。

**所以本文件记的不是结论，而是**：
1. **探针本身是好的** ✓（三条判据、从真 UIA 树取、带脱敏 ✓）—— 它**能**回答这个问题 ✓；
2. **要在 `#239` 之后重新构建并装一次，再跑它** ✓ —— **这是待办** ✓，**不是已完成** ✗。

---

## 1. 探针做了什么（可复跑 ✓）

三条判据（都从**真应用经 WebView2 暴露的 UIA 树**取 ✓，**不是读源码** ✗）：

| # | 判据 | 期望 |
| --- | --- | --- |
| ① | 每份结果是不是**标题元素**（`HeadingLevel` 有值 ✓） | 有标题的结果数 = 总结果数 |
| ② | 每个 `<ul>` 的**可访问名字** | `一共 N 份结果` ✓ |
| ③ | 顺手量**确认页**那一屏的标题 | （对照组）|

「一份结果」的锚点：面板里每份结果是一个 `ListItem`（`<li>` ✓），它的**第一个标题后代**就是这一份的名字 ✓ —— 这样**不依赖 CSS 类名** ✓。

跑法（带超时 ✓）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File gui\scripts\windows\a11y\probe-result-headings.ps1 `
  -Exe <安装目录>\cante-gui.exe -OutDir <输出目录>
```

输出 `result-headings.json` ✓（结构化）与 `result-headings.txt` ✓（人看）。

---

## 2. 这一轮的原始数字（**旧构建** ✓，只作对照 ✓）

```json
{ "rows": 12, "withHeading": 0, "lists": 1, "ulNamed": 0 }
```

**为什么不能拿它当"修复前基线"** ✗：它量的构建**比 `#239` 还早** ✓，而 `-22.md` 那份"修复前"量的也是同一代构建 ✓ —— 两者**都不含 `#239`** ✓，所以**它们之间的差异说明不了 `#239`** ✗。

---

## 3. 从"量错东西"这件事本身得到的（**唯一有价值的产出** ✓）

**一个可复用的核对步骤** ✓ —— 以后每次真机复验前，先回答：

```powershell
# ① 被测 exe 的构建时间
(Get-Item $exe).LastWriteTime
# ② 它里面有没有这次改动的痕迹（挑一个这次新加的、用户可见的字符串 ✓）
Select-String -Path $exe -Pattern "一共" -Encoding UTF8 -List   # 应命中
```

**②这一步是这次教训的关键** ✓：光看"时间是几点"要靠人对时间 ✓，**grep 一个这次新加的文案**才是一眼可判的判据 ✓。**下次复验请先跑这两条** ✓。

---

## 4. 没做到的（如实 ✗）

- **`#239` 的真机复验**：**没做成** ✗（量的构建太旧 ✓）—— **不是"没通过"** ✓，是**没验** ✓；
- **`-22.md` 那份"修复前基线"同样量的是不含 `#239` 的构建** ✓ —— 所以**"修复前 vs 修复后"这个对照目前还不存在** ✗，要**先重建再重跑** ✓；
- **本轮每场只跑了一次** ✓（且这一轮本身作废 ✓）。
