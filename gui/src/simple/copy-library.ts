// 「能力中心」（你要做什么？）的全部中文文案。
//
// 和 copy.ts 一样：这里只放面向用户的中文，一条术语都没有。之所以单独一个
// 文件，是为了不和别的 workstream 正在改的 copy.ts 抢同一行。
//
// 文案的基调是「平实、有依据」：每一项都要说清楚「需要什么」「会动到什么」
// 「现在能不能做」。最后一条尤其重要——做不到的事必须说实话，而不是等用户
// 等到最后才失败。
export const LIBRARY = {
  // ---- 首页入口 ----------------------------------------------------------
  /** 入口那一句问话，和按钮连起来读：「还有别的事？看看能做什么（共 N 项）」。 */
  entryHint: "还有别的事？",
  entryBody: "按你想做的事搜一搜，每一项都写清楚要准备什么、会动到什么。",
  entryButton: (count: number) => `看看能做什么（共 ${count} 项）`,

  // ---- 浮层顶部 ----------------------------------------------------------
  title: "你要做什么？",
  close: "回到首页",
  searchLabel: "用一句话说说你想做什么",
  searchPlaceholder: "比如：把几张表合成一张、把文件按月份分好",

  // ---- 结果卡片 ----------------------------------------------------------
  /** 有搜索词时的结果标题。 */
  hitsTitle: (count: number) => `找到 ${count} 项`,
  /** 没有搜索词时，按组展示全部。 */
  allTitle: "能做的事",
  needsLabel: "需要什么",
  needs: {
    files: "需要你选几个文件",
    folder: "需要你选一个文件夹",
    none: "不需要给文件",
    text: "需要你打字说一句",
  },
  risksLabel: "可能不准的地方",
  /** 只显示第一条风险，其余折叠在这句话后面。 */
  risksMore: (count: number) => `还有 ${count} 条`,
  risksLess: "收起",

  // ---- 诚实的边界 --------------------------------------------------------
  /**
   * 现在这台电脑做不到的事。这不是错误，所以界面用中性的提示，不用红色。
   * 判断逻辑在 catalog.ts 的 availabilityHint：需要读图片里的字的任务。
   */
  unavailableImage: "现在这台电脑还不能读图片里的字，需要先想办法把内容变成文字",

  // ---- 搜不到 ------------------------------------------------------------
  emptyTitle: "没找到你要做的事",
  emptyBody: "换个说法再搜一次，或者回首页用一句话说给我听，我照着做。",
  emptyRetry: "换个说法再搜",
  emptyHome: "回首页说一句话",
} as const;
