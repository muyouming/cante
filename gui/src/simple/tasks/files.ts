// File housekeeping and PDF jobs (#49, #50).
//
// Nothing here ever deletes. Renaming copies (the original stays), tidying only
// moves within the chosen folder, and the duplicate finder only writes a list —
// the undo layer is the safety net, but the prompt should not need it.
import type { TaskDef } from "./types.ts";
import { buildPrompt } from "./prompt.ts";

/** #49 (first) — a whole pile of files gets readable names. */
const rename: TaskDef = {
  id: "files.rename",
  title: "按规则改成新名字（原来的留着）",
  example: "把这些照片复制一份，按「2024年5月_序号」重新命名",
  group: "文件",
  needs: "files",
  plan: [
    "先算出每个文件准备改成的新名字，把对照表写进结果里",
    "按新名字复制出一份，原来的文件原样留着",
    "新名字已经有人用了，就自动加序号，绝不覆盖",
    "改名清单另存为一个新文件",
  ],
  risks: [
    "你给的改名规则如果说不全（比如用哪个日期、序号从几开始），我会先问你，不会自己定。",
    "从 1 开始编号时，按什么顺序编（文件名的先后、还是你选文件的顺序）如果没说清楚，我会自己定一个，排出来的序号可能和你想的不一样；不对的话请你告诉我，我按你说的顺序重排。",
    "新名字和已有文件重名时，我会自动加「_2」「_3」，加出来的名字可能和你预想的不一样；加了序号的是哪几个，我会在清单里标出来。",
    "照片如果没有拍摄日期的记录，我不会从文件名里猜一个日期，会停下来问你要用哪个日期。",
    "改名其实是复制出一份新名字的，原来那份一个都不动，所以整理完文件会多出一份；多出来的那份留不留由你决定。",
  ],
  prompt(files, instruction) {
    return buildPrompt({
      what: "把这些文件按用户说的规则改成新的名字。为了不弄丢东西，请复制出一份新名字的文件，原来的文件留着不动。",
      files,
      how: [
        "先把每个文件原来叫什么、准备改成什么，列成一张对照表，写在回复的最前面（这份对照表最后要写进清单文件）。",
        "对照表写完后接着往下做，不要停下来等我回复；只有哪个文件的新名字你确实拿不准，才停下来问我一句。",
        "命名规则以用户原话为准。用户说得不完整的地方（比如序号从几开始、日期用哪个日期），先问我，不要自己定。",
        "复制出新名字的文件，放在原文件旁边。原来的文件一个都不要改名、不要删、不要移动。",
        "如果新名字已经被别的文件占了，就在后面加「_2」「_3」这样的序号，绝对不要覆盖任何已有文件。",
        "文件里的内容一个字都不要改。遇到打不开的文件先跳过，并在最后告诉我。",
        "另外把对照表存成一个新文件，放在原文件旁边，文件名以「结果_改名清单」开头，里面写清楚：原文件名、新文件名、放在哪个文件夹。",
      ],
      instruction,
      done: "一共改了多少个文件、有几个因为重名自动加了序号、哪些文件跳过了、清单文件的完整位置。",
    });
  },
  summaryHints: ["一共改了多少个文件", "有没有重名自动加了序号", "清单文件在哪个位置"],
};

/** #49 (second) — sort a folder by file type. */
const archive: TaskDef = {
  id: "files.archive",
  title: "按文件类型分到不同文件夹",
  example: "把这个文件夹里的文件按图片、视频、文档、表格分开整理",
  group: "文件",
  needs: "folder",
  plan: [
    "先扫描一遍文件夹（子文件夹里的文件也算），把出现的文件类型数清楚",
    "按类型建好文件夹（图片、视频、文档、表格、其它）",
    "把文件挪进去，遇到同名文件自动加序号，绝不覆盖",
    "整理清单另存为新文件，不删任何东西",
  ],
  risks: [
    "按扩展名分类：没有后缀的文件会放进「其它」；压缩包、安装包这类拿不准的会留在原地不动，最后一起告诉你。",
    "子文件夹里的文件也会一起挪出来分类（比如「2025年」里的照片会挪到「图片」），不想动哪个子文件夹请先说一声。",
    "文件夹里本来就有同名文件夹时，文件会并进去（不会另建一个）；并进了哪几个，我会在清单里写清。",
    "正被别的程序打开的文件挪不动，会留在原地，我会在最后告诉你哪几个没有动。",
    "整理是把文件挪进分类文件夹，挪完原来那里就没有了；每个文件从哪挪到哪我会在清单里写清，看不过去可以用结果页的「一键撤销」搬回原位。",
  ],
  prompt(files, instruction) {
    return buildPrompt({
      what: "按文件类型，把文件夹里的文件分别归到几个子文件夹里。",
      folder: files[0] ?? "",
      how: [
        "先扫描一遍整个文件夹（包含子文件夹，但不要跟进已经建好的分类文件夹里再分类），把有哪些类型、各有多少个，写在回复的最前面，然后接着往下做，不要停下来等我回复。",
        "建好这几个分类文件夹：图片、视频、文档、表格、其它。按文件扩展名判断类型；拿不准的丢进「其它」。",
        "把文件挪进对应的文件夹。原来的文件夹里已经有的分类文件夹不要重复建。",
        "遇到新位置已经有同名文件，就在名字后面加「_2」「_3」这样的序号，绝对不要覆盖任何已有文件。",
        "文件内容一个字都不要改，只挪位置。压缩包、安装包、快捷方式之类不确定的，先别动，最后单独告诉我。",
        "把整理清单存成一个新文件，放在这个文件夹里，文件名以「结果_整理清单」开头，写清楚：哪个文件从哪挪到了哪。",
      ],
      extraRule:
        "这个任务需要把文件挪到分类文件夹里。所以「不要移动原文件」这一条改成：只挪动到本文件夹内新建的分类文件夹，不要删除、不要覆盖、不要改内容；打算怎么挪的清单写在回复开头，然后直接做完，不要为了等我看清单而停下来。",
      instruction,
      done: "一共整理了多少个文件、分成了哪几类、每一类多少个、有没有重名、哪些文件你没动、清单文件的完整位置。",
    });
  },
  summaryHints: ["一共整理了多少个文件", "分成了哪几类", "有没有重名", "清单文件在哪个位置"],
};

/** #49 (third) — find copies, delete nothing. */
const dupes: TaskDef = {
  id: "files.dupes",
  title: "找出重复的文件，列一份清单",
  example: "帮我找出这个文件夹里内容一样的文件，列个清单",
  group: "文件",
  needs: "folder",
  plan: [
    "扫描文件夹里的文件，先按大小分组，省掉大部分比较",
    "大小一样的再比内容，内容完全一致才算重复",
    "每一组重复保留哪一份给个建议，其余列进清单",
    "清单另存为新文件，不删任何文件",
  ],
  risks: [
    "内容一样、但重新保存过一次的文件（大小差一点点），我只会单独提醒，不算作重复。",
    "图片和视频只比内容，画面像但大小或清晰度不同的，不算重复；这类我会单独列出来让你自己看一眼。",
    "「留哪一份」只是我的建议，我一个文件都不会删，删不删由你决定。",
    "「建议留哪一份」是按文件名和位置给的；很多文件的创建时间一模一样（复制粘贴常见），我不会硬按时间排，会写明这份建议是按什么给的。",
  ],
  prompt(files, instruction) {
    return buildPrompt({
      what: "找出文件夹里内容完全一样的文件，列成一份清单。这个任务只找、只列，不要删除或移动任何文件。",
      folder: files[0] ?? "",
      how: [
        "先把整个文件夹里的所有文件扫一遍（包含子文件夹），把每个文件的大小和完整位置记下来。",
        "先按大小分组，大小不一样的肯定不是重复，省掉比较。大小一样的，再一份一份比对内容，内容完全一致才算重复。",
        "图片和视频这类文件，如果内容一样但被重新存过一次（大小略有差别），也单独列出来提醒我，但不要和「完全一样」混在一起。",
        "每一组重复文件：列出这一组有几个、每个的完整位置、每个多大、内容完全一样到哪一步。",
        "每一组给出建议：留哪一份（建议留最早创建或者位置最清楚的那一份），其余的建议删掉。但只写建议，不要真的删。",
        "把清单存成一个新文件，放在这个文件夹里，文件名以「结果_重复文件清单」开头。",
      ],
      instruction,
      done: "找到几组重复文件、一共多出来多少个、大概能省下多少空间、你对每一组建议留哪一份、清单文件的完整位置。",
    });
  },
  summaryHints: ["找到几组重复文件", "一共多出来多少个", "大概能省下多少空间", "清单文件在哪个位置"],
};

/** #50 (first) — several PDFs become one. */
const pdfMerge: TaskDef = {
  id: "pdf.merge",
  title: "把几个 PDF 合成一个",
  example: "把这几份材料合成一个 PDF，按我选的顺序",
  group: "文件",
  needs: "files",
  accept: ["pdf"],
  plan: [
    "先打开每个 PDF，确认都能正常读取、各有多少页",
    "按你选的顺序首尾相接，合成一个",
    "结果另存为一个新 PDF，原来的文件都不动",
  ],
  risks: [
    "其中一份 PDF 加了密码或已经损坏时，我会停下来问你，不会跳过它。",
    "扫描件（整页是图片的 PDF）合并后还是图片，里面的字选不中、也搜不到；要能搜字的版本，请你先单独说一声，合并这一步做不到。",
    "合并只把页面首尾相接，原来的页码不会重新编，可能出现两个第 3 页；我会在结果里写明一共多少页、每份原来各多少页。",
    "PDF 里原来加的书签，合成之后不一定都还在；要是你靠书签翻页，请你合并后翻一遍，缺了告诉我。",
  ],
  prompt(files, instruction) {
    return buildPrompt({
      what: "把下面的 PDF 按顺序合成一个 PDF。",
      files,
      how: [
        "先逐个打开，在回复开头告诉我每个文件能不能正常读取、各有多少页。打不开或加了密码的，先停下来问我，不要跳过去不管。",
        "按上面列出的顺序首尾相接合成一个。用户如果说了别的顺序，以用户原话为准。",
        "合成时不要压缩画质、不要裁掉页边、不要重新排版；每一页保持原来的样子。",
        "如果需要核对页里的文字：用 cante-pdf 抽出来如果读不成句子，那就是乱码（这种文件它不一定报警），不要拿它下结论；只看页数和顺序就够。",
        "如果文件里有加了书签或页面编号的，保持原样；合成后的文件名以「结果_合并」开头。",
        "结果另存为一个新 PDF，放在第一个文件旁边。原来的 PDF 一个都不要改、不要删、不要覆盖。",
      ],
      instruction,
      done: "一共合并了几个文件、每个多少页、合成后一共多少页、有没有打不开的文件、结果文件的完整位置。",
    });
  },
  summaryHints: ["合并了几个文件", "合成后一共多少页", "有没有打不开的文件", "结果文件在哪个位置"],
};

/** #50 (second) — one PDF becomes several. */
const pdfSplit: TaskDef = {
  id: "pdf.split",
  title: "把一个 PDF 拆成几份",
  example: "把这份 PDF 每一页拆成一个文件",
  group: "文件",
  needs: "files",
  accept: ["pdf"],
  plan: [
    "先读一遍，确认总页数和每一页有没有内容",
    "按你的要求拆分（每页一份、每几页一份、或者指定页码）",
    "每一份都另存为新文件，放在一个新文件夹里",
    "原来的 PDF 不动",
  ],
  risks: [
    "拆出来的每一份保留原来的页码，所以每一份可能从第 5 页开始，不会从 1 重新编；我会在每一份的文件名里写清它包含第几页到第几页。",
    "空白页我不会自动去掉，会按原样拆出来。",
    "加了密码的 PDF 打不开，我会停下来问你，不会硬拆。",
    "如果整份是扫描件（选不中文字），拆分照旧；只是拆出来的每一份仍然不能搜字、不能选字，要能搜字的版本请你先单独说一声，拆分这一步做不到。",
  ],
  prompt(files, instruction) {
    return buildPrompt({
      what: "把一个 PDF 按用户的要求拆成几份。",
      files,
      how: [
        "先读一遍，在回复开头告诉我总共有多少页，有没有空白页。如果给的文件不止一个，先问我要拆哪一个。",
        "拆分方式以用户原话为准：可能是每一页一份、每几页一份，也可能是按页码范围。用户没说清楚就先问我，不要自己定。",
        "每一份都是完整的一页或几页，不要改内容、不要重新排版、不要降画质。",
        "判断空白页和页数时，只看页数这一项就够。用 cante-pdf 抽文字如果读不成句子，那就是乱码（这种文件它不一定报警），不要把乱码当成「这一页没内容」。",
        "另外建一个新文件夹，名字以「结果_拆分」开头，放在原 PDF 旁边，每一份按顺序命名并写清包含第几页到第几页。",
        "原来的 PDF 不要改、不要删、不要覆盖。",
      ],
      instruction,
      done: "一共拆成了几个文件、每个文件有多少页、新文件夹的完整位置、有没有空白页。",
    });
  },
  summaryHints: ["拆成了几个文件", "每个文件多少页", "新文件夹在哪个位置"],
};

/** #50 (third) — an editable document out of a PDF. */
const pdfToWord: TaskDef = {
  id: "pdf.toword",
  title: "把 PDF 转成 Word 文档",
  example: "把这份 PDF 转成 Word，我要改里面的字",
  group: "文件",
  needs: "files",
  accept: ["pdf"],
  plan: [
    "先读一遍 PDF，看它是文字版还是扫描的图片",
    "是扫描件就先停下来问你要不要逐字识别",
    "把文字和表格按原来的顺序提取出来，尽量保留段落和表格",
    "能生成 Word 就生成 Word；这台电脑做不出来，就把文字交给你自己粘贴，结果同样另存为新文件，原 PDF 不动",
    "有看不清的地方单独列出来提醒你",
  ],
  risks: [
    "扫描件（整页是图片、选不中文字的 PDF）转成 Word 可能变成乱码，这种情况我会先停下来问你，不会硬转。",
    "文字版的 PDF 里，表格的框线和合并格子可能还原不齐；我会在最后「需要你核对」里写清哪几个表格没还原好。",
    "页眉、页脚、页码、水印在 Word 里可能跑到正文中间，或者重复出现；这类内容我会单独列出来，问你要不要保留。",
    "这份 Word 是我把文字重新排出来的，不是把原 PDF 包了一层；打开后版面可能和原件有差别，请你对着原件核对一眼再改。",
    "这台电脑上如果没有能生成 Word 文件的工具，我不会假装转好了：会把文字直接给你，并告诉你怎么新建一个 Word 文档、把文字粘贴进去。",
  ],
  prompt(files, instruction) {
    return buildPrompt({
      what: "把下面的 PDF 转成一个可以编辑的 Word 文档。",
      files,
      how: [
        "先读一遍 PDF，判断它是文字版还是扫描图片版，然后在回复开头告诉我。",
        "如果是扫描的图片、里面没有可以选中的文字，先停下来告诉我：这种要逐字识别，会有出错的地方，需不需要继续。不要硬转出一堆乱码。",
        "用 cante-pdf 抽文字时，如果抽出来读不成句子（比如一串莫名其妙的符号），那就是乱码，不代表原件没内容——这种文件它不一定报警。这时先停下来告诉我，问我要不要逐字识别，不要拿乱码去拼 Word。",
        "是文字版的话，按原来的顺序把文字和表格提取出来，段落怎么分就怎么分，表格尽量还原成表格。",
        "标题、正文、编号列表按原来的层级保留：一级标题还是一级标题，不要全部变成一样大的字。",
        "页眉页脚、水印、页码这类重复出现的内容，单独告诉我，问我要不要保留。",
        "这台电脑上只有确实能生成 Word 文件（.docx）的工具时才真的去生成 Word；不确定有没有这样的工具，就不要试，改成把整篇文字按顺序直接写在回复里，并告诉用户怎么新建一个 Word 文档、把文字粘贴进去。",
        "如果你没有真的生成出 Word 文件，就在【需要你核对】里写明「结果只是文字，还没有变成 Word 文件」，绝对不要说已经转成 Word 了。",
        "结果另存为一个新文件：真的生成了 Word 就存成 .docx，没生成就把文字存成一个 .txt 文本文件，放在原 PDF 旁边，文件名都以「结果_转换」开头。原来的 PDF 不要改、不要删、不要覆盖。",
      ],
      instruction,
      done: "一共转了多少页、哪些地方识别得不确定、要不要人工核对、结果文件的完整位置。",
    });
  },
  summaryHints: ["一共转了多少页", "有没有识别不准的地方", "结果文件在哪个位置"],
};

export const FILE_TASKS: TaskDef[] = [rename, archive, dupes, pdfMerge, pdfSplit, pdfToWord];
