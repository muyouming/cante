// 做完一件事之后的「下一步」文案。
//
// 王姐只会复制粘贴和求和。她做完一件事、看到「做好了」之后，常常不知道还能做什么
// ——而产品最值钱的地方，正是把「这件事之后顺手能做的那件事」摆到她面前。
//
// 两种真事：她刚把三张表合并成一张，下一步多半要一份能交上去的月报；她刚把照片上
// 的表格抄成 Excel，下一步多半是把格式理干净。这里放的正是这几句。
//
// 两条死规矩：
//   * 只说**我们已经能做**的那件事——每条建议指向目录里真有的卡片，判断逻辑在
//     followup.ts，测试会拿 `instructionFor` 拼出真正发出去的那段指令来核对；
//   * 绝不出现「自动发送 / 自动群发」——微信那条红线里不允许存在发送路径。
//
// 和别的 copy 模块一样：面向用户的每一句都在这儿，全中文、零术语。
// 按钮文字刻意短（2–4 个字），因为她要的是一眼能看懂、一点就开始。

export const NEXT_STEP = {
  /** 这一节的标题：说清这是「刚才那件事」的下一步。 */
  heading: "这件事做完了，接下来可以顺手做：",
  /** 一句说明：点一下会发生什么，用的是哪份东西。 */
  hint: "点一下接着做，会用刚做好的这份结果。",

  steps: {
    /** 刚合并完明细表 → 出月报（按类别汇总并配上图表）。 */
    monthly: {
      label: "做成月报",
      say: "把刚做好的这张表按部门和月份汇总，再配一张图。",
    },
    /** 刚把照片/截图抄成 Excel → 把格式理干净。 */
    tidy: {
      label: "整理格式",
      say: "把刚做好的这张表整理一下：表头、空行、日期格式都理一理。",
    },
    /** 刚把发票汇总成台账 → 查一下有没有重复。 */
    dupes: {
      label: "找重复",
      say: "看看刚做好的这份台账里有没有重复的发票，只标出来，别删。",
    },
    /** 刚把两张表对完账 → 把对不上的那几行单独挑出来存一份。 */
    onlyDiff: {
      label: "挑出差异",
      say: "从刚做好的对账结果里，把对不上的那几行单独挑出来，另存一张新表。",
    },
  },
} as const;
