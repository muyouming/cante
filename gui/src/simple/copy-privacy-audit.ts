// r20 隐私审计：「这次发出去了什么」这一节的文案。
//
// 起因：隐私面板一直只说「发给谁」，没说「发出去的是什么」。而调研里「不敢用」的
// 第一条理由就是数据。这一节把真正发出去的那段文字原样摊开给她看——它本来就是
// 本地拼出来的（store.composedInstruction），没有理由藏着。
//
// 硬约束：全中文、零术语（copy-guard.test.ts 扫这一整个模块）。特别注意不要写
// 「路径」，要用「文件位置」；也不要说「模型 / 会话 / 缓存」这类词。
//
// 这一节只展示「发出去的那段文字」，绝不读取或展示任何本地文件内容——这条边界
// 写在 privacy.ts 的注释里，改这里之前先看那段。

export const SENT = {
  /** 这一节的标题，和面板里其它小节一样用一句平实中文。 */
  title: "这次发出去了什么",

  /** ① 文件本身一直在本机，我们不会把整个文件夹打成一包传走。 */
  filesStay: "你的文件一直在这台电脑上，我们不会把你的整个文件夹传出去。",
  /** ② 发出去的到底是哪一段；顺带说实话：里面会有文件名和它放在哪。 */
  textGoes:
    "发出去的就是下面这段文字，为了让帮你整理的服务方看懂你要做什么；里面会有你选的文件叫什么、放在哪。",
  /**
   * 发过不止一回（确认页一次 + 追问过几轮）时，先单起一句点明默认展示的是
   * **最近一次**——她追问过之后再来看，就不会拿上一次的旧文字冒充。
   *
   * 刻意短，且和 `textGoes`（那句仍然照常显示）分开：可读性预算表只许减不许增。
   */
  textGoesLatest: "你发过不止一次，下面这段是最近一次发出去的。",

  /** 最近一次那段摘要前面的几个字：说清默认视图问的是哪一次。 */
  latestSummary: (summary: string) => `最近一次：${summary}`,

  /** 发过不止一次时，展开开关上面那一行。 */
  earlier: (count: number) => `之前还发过 ${count} 次。`,
  /** 「之前还发过 N 次」的开关：点开是每一次的原文，按发生先后的顺序。 */
  historyShow: "看全部发过的文字",
  historyHide: "收起全部文字",

  /** 历史里每一段文字的抬头：第几次、什么时候发的。 */
  turnHeading: (index: number, when: string, latest: boolean) =>
    latest ? `第 ${index} 次（最近一次）· ${when}` : `第 ${index} 次 · ${when}`,
  /** ③ 这段文字是本地拼出来的，她可以自己核对。 */
  textIsLocal: "这段文字是在这台电脑上拼出来的，你可以自己看、自己判断。",

  /** 还没跑过任务。 */
  none: "还没有做过任务。等你做过一次，这里会显示那次发出去的文字。",
  /** 跑过任务，但那一次只在本地处理。 */
  offline: "这次什么都没发出去：内容只在这台电脑上处理。",

  /** 折叠 -> 展开 的按钮：展开的是**最近一次**那段的原文。 */
  showFull: "看最近这段文字",
  hideFull: "收起文字",

  /** 结果卡片上的入口；她刚做完一件事，最想问的就是「刚才发了什么」。 */
  entryShow: "看看刚才发出去的是什么",
  entryHide: "收起刚才发出去的文字",

  /** 摘要里那几类内容的说法，顺序固定。 */
  partLabel: {
    what: "要做的事",
    where: "文件位置",
    how: "怎么做",
    words: "你的原话",
  },

  /** 折叠时的一句话摘要：多少字、里面有哪几类内容。 */
  summary: (length: number, parts: readonly string[]): string =>
    parts.length === 0
      ? `这段文字一共 ${length} 个字。`
      : `这段文字一共 ${length} 个字，里面有：${parts.join("、")}。`,
} as const;

/**
 * 确认页上那句「这次内容去哪了」。
 *
 * 走查发现的缺口：动手前那张确认页（ConfirmSheet）只念了「它打算这样做」+ 文件清单，
 * 一个字都没说这次内容会不会发出去、发给谁 —— 而那正是她按下「开始」之前最后看一眼
 * 的地方。面板里那三句（文件不出去 / 发出去的是这段文字 / 这段文字是本机拼的）说得
 * 最全，所以这里按那三句补齐，两处口径一致。
 *
 * 只在本机时不提服务方（与面板的「什么都没发出去」同一个意思）；联网时把服务方的
 * 名字原样带上（和面板的「联网时内容发给谁」用同一个值）。
 */
export const EGRESS = {
  /** 确认页上这一小节的标题。 */
  heading: "这次内容去哪了",

  /** 只在本机处理：和面板的「什么都没发出去」是同一件事。 */
  local: "这次只在这台电脑上处理，什么都不会发出去。",

  /** 联网、还不知道服务方叫什么（她还没跑过任务）。 */
  online: "这次要联网：发出去的是这段文字，是在这台电脑上拼出来的。",

  /** 联网、已经知道服务方名字。名字原样带出，和面板用同一个值。 */
  onlineNamed: (who: string) =>
    `这次要联网：发出去的是这段文字，是在这台电脑上拼出来的。发给帮你整理的服务方：${who}。`,

  /** 无论联不联网都成立的那半句：她最担心的那个 —— 文件本身不走。 */
  filesStay: "你的文件一直在这台电脑上。",
} as const;

/**
 * 「它什么时候会动手」——这一节回答的是她**没说出口的那个担心**：
 * 「我不在的时候，它会不会自己偷偷干活？」
 *
 * 面板此前只讲了「内容去了哪」（数据流向），没回答这个问题；而对一个
 * 45 岁、本来就不太敢点的人来说，这句才是她真正会担心的。
 *
 * 两句话都不是修辞，各自对应一条**已被测试钉住的行为**：
 * ① 不点「开始」不动手：一次运行要先暂存、等她点头才开跑
 *    （`store.startRun` 的注释：Nothing runs until `confirmRun`）；
 * ② 不用它的时候不自己干活：定时任务只把要确认的事**排到首页**，绝不替她按开始
 *    （`store.test.ts` 里那段产品律：排队不等于自动做完；每件都要先摆到确认页，
 *    她点了「开始」才动手）。
 *
 * 改这两句之前，先去看那两条行为还在不在——文案与行为必须是同一件事。
 */
export const WHEN = {
  title: "它什么时候会动手",
  onlyAfterYouSay: "你不点「开始」，它不动手。要做的事会先摆出来给你看，你点头了它才开始。",
  notInBackground:
    "你没在用它的时候，它也不会自己干活。定时的那几件，只会把「要不要做」排到首页，还是要你自己点了才会做。",
} as const;
