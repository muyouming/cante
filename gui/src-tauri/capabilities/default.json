{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions for the Cante client.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
