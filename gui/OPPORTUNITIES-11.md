# 第 17 批机会点：**修好的东西只有进了安装包才算到了她手上**

起点：第 16 批（#284–#290）全部合并 ✓、main **1224 pass / 0 fail** ✓、0 开着 ✓。
这一批的机会点**全部来自第 16 批证据文件里的"没验的"小节** ✓ —— 不用编 ✓。

---

## P1 · 装机版还带着已修的缺口 ✗ —— 发 0.2.4（**本批主活 ✓ 已开跑**）

**判据（验收 6 §0 的实测 ✓）**：装机版 = 09-20 **02:27** ✓，比 #279（20:40 ✓）与
#282（23:58 ✓）**都早**；**版本号同为 0.2.3 分不出新旧、只有时间戳分得开** ✓。
也就是说**此刻真装在用户机器上的，还是会把英文原话端上屏的那一版** ✗。
**要做**：三处版本号 → 0.2.4 ✓（PR #291 ✓）→ tag `gui-v0.2.4` ✓ → release 双平台产物 ✓ →
**Windows 装机后复跑 `accept-gbk-csv.ps1`**（#287 固化的 ✓）验"装机版真的修了" ✓
—— 这条闭环（**下载真实产物 → 装上 → 复跑判据**）以前从来没走完过 ✗。

## P1 · 结果面板的「复制位置」**真窗口没验到**（#290 的收尾 ✓）

**判据**：#290 给了面板每行「复制 X 的位置」✓，本地渲染断言全过 ✓，
但**真窗口 + 真 `Get-Clipboard`** 还没人点过 ✗（验收 7 §5 原话：
"不能拿别的复制顶替" ✓）。验收 7 已把路铺好（夹具起得来 ✓、seed-result-rows 能造行 ✓）。
**要做**：Windows agent 键盘点开 → `Get-Clipboard` 逐字存 ✓（首=盘符 ✓ 尾=.xlsx ✓
反斜杠 ✓ 无正斜杠 ✓）+ 成功提示逐字 ✓。（**进行中 ✓ CanteR18 ✓**）

## P2 · 验收 7 §4 记的那条**结构性事实**：ResultCard 到不了 ✗

夹具不写盘 → 快照 diff 空 → **跑完一轮的 ResultCard 在夹具模式下永远不出现** ✓。
这不只是验收问题——**dom-smoke 也永远演不到那一屏** ✓。
**要做**：让 `fake-cante` 有一种状态**真的往磁盘写一个结果文件** ✓
（写进系统临时目录 ✓ 测试后清掉 ✓），这样 ResultCard（带它全部按钮 ✓）
进得了自动化 ✓。**这一条不做也不影响产品** ✓（#290 已把面板那条主动线补上 ✓）；
它影响的是**我们回归的覆盖面** ✓。所以排 P2 ✓、量小 ✓ 见缝插针 ✓。

## 已核：**不是缺口** ✗

| 怀疑 | 核过的事实 | 结论 |
| --- | --- | --- |
| 面板按钮挤坏布局 | #290 e2e 9/9（dom-smoke 800×600 真量 ✓）| **已挡** ✓ |
| 文案在卡/面板分叉 | #290 复用同一句 `copyLocation` + 同一 `pathForClipboard` ✓（断言锁"同一句"✓）| **已有** ✓ |
| glib 依赖告警（Dependabot #67 ✓）| `gui/docs/DEPENDENCIES.md` **有据接受** ✓（带复查条件 ✓）| **已处理** ✓ |

## 边界（不做 ✗）

不为 0.2.4 塞任何新改功能（发版就是发版 ✓ 只动版本号 ✗）；
不"顺手重构"；不碰红线（复制≠发送 ✓）；不为通过放松闸门 ✓。
