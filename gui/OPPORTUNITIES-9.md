# 第 15 批机会点：**备份会把她 C 盘吃满**

起点：第 14 批（#272/#273/#274）已合并 ✓，0 开着 PR / 0 开着 issue ✓。
这一批的起点不是"我又想到一个"✓ —— 是**第 14 批的 agent 自己挖出来的事实** ✓
（`gui/docs/SINGLE-INSTANCE.md` 说快照目录无归属 ✓；顺着往下核，发现更硬的一条 ✓）。

---

## P1 · 备份目录**只写不删**，能把她的磁盘吃满 ✗（**新**，但根在 #274 那次核查里）

**判据（逐条核过 ✓）**：
1. 每次运行在 `file-safety/runs/<编号>/before/` 留一份**原件备份** ✓（`files.rs:37` ✓）；
2. **单次**有上限 ✓（2000 文件 / 单文件 32MB / 合计 256MB ✓，超出只记 `unbacked` ✓）——
   这一层做得对 ✓；
3. 但**跨次没有任何清理** ✗：全文件 `remove_dir_all` **只出现 0 次** ✓（`UNDO-BOUNDS.md:19`
   自己就写着「没有任何按时间/天数的清理」✓）；
4. 运行记录 `truncate(MAX_RUNS = 200)` ✓（`files.rs:478` ✓）—— **只丢记录，不删目录** ✗；
5. 于是：**第 201 次之后的每次运行，都会留下一个再也没人引用、也永远不会被删的备份目录** ✗
   —— 每个最多 256MB ✓。
6. 我本机那份 `file-safety` 现在是 **4KB / 0 个 run 目录** ✓（干净机器 ✓），
   所以这**不是"已经出事"** ✓，而是**随着她用会慢慢积累** ✓。

**为什么是 P1（而且正好打在产品律上）**：产品律 2 是「**别弄坏我的东西**」✓。
把她的 C 盘一点点吃满 ✓，最后的表现是"电脑变卡"✓、"别的程序存不了东西"✗ ——
**而她完全不会想到是 cante 干的** ✗（她连"备份"这个词都没见过 ✓）。
这比一个显式的错误更坏 ✗：它**无声** ✓。

**要做**（**先核，再定策略** ✗）：
- 核清**今天到底会不会真的积累** ✓（含：`unbacked` 的路径是不是也建目录 ✓、
  删除的记录对应的目录是不是就留着了 ✓、重装/升级会不会清 ✓）；
- 然后定**保留策略** —— 但要同时守住两件互相拉扯的事 ✓：
  ① **她随时能撤销**（撤销要读 `before.json` ✓，不能在她说"撤销"时发现备份没了 ✗）；
  ② **不该无限占她的空间** ✓。
  这一条**必须留出"她看得懂"的口子** ✓：要么在**动手前**就说清"这次会多占多少"✓，
  要么在**清理**时留下**她能看到的一句** ✓（绝不能悄悄删 ✗）。
- 结论写进 `gui/docs/BACKUP-GROWTH.md` ✓，**逐条给依据** ✓，核不出来的写"没核出来" ✗。

## P2 · 同一次运行里，两个进程会争同一个临时文件名 ✗（**新**，也是 #274 挖出来的）

**判据**：`files.rs:436` 的 `path.with_extension("json.tmp")` ✓ 是**固定名** ✓ ——
两个进程（单实例已挡住 GUI 主路径 ✓，但守护进程侧没有锁 ✗）同时写就是**互相覆盖** ✓。
**要做**：**先核**实际发生率 ✓（单实例之后还剩哪些入口 ✓），再决定修不修 ✓；
修的话用**带随机后缀的临时名 + 原子改名** ✓（`write_json` 那一处 ✓）。

## 已核：**不是缺口** ✗（省得下轮再查）

| 怀疑 | 核过的事实 | 结论 |
| --- | --- | --- |
| 她说"没动静"以为卡死 | `copy-wait.ts` ✓ 做得**极细**：只用**观察来的**范围 ✓、**不打包票** ✓（「一般要」不是「最多」✓）、超过九成位改口「比一般情况久」✓、不说"连不上"（那一屏有别的文案管 ✓）| **已有**，质量很高 ✓ |
| 「00123」被当成数字 | `sheets.rs:116/345` ✓ 前导 0 保留 ✓；`:796` ✓ 一整排边界（`1e3`/`12%`/`空格`/`五` ✓）都**不**当数字 ✓；`iso_date_literal` ✓ 也有一排 ✓ | **已有** ✓ |
| 磁盘真写不下时说什么 | `copy.ts:170 spaceWhat` ✓ + `:269` ✓ 有出口 ✓ | **已有** ✓（本批补的是**预防**，不是这句 ✓）|
| CSV 往返丢格式 | `sheets.rs:426/437/499` ✓ + 测试 `csv_round_trip_is_stable` ✓ / 引号逗号换行 ✓ | **已有** ✓ |
| 结果文件被 Excel 占着 | `copy-recovery.ts:94` ✓「关掉那个窗口再试」✓ | **已有** ✓ |
| 做完才发现选错文件 | `copy-recovery.ts:84/89` ✓「重新选一次 / 换一份文件再试」✓ | **已有** ✓ |

---

## 边界（不做 ✗）

不碰打包段 / workflows / 专业模式 ✓；**不做**任何**悄悄**的清理 ✗（要清就必须有她能看到的一句 ✓）；
不为了通过而放松既有闸门（copy / typography / tone / platform / a11y / 秘密扫描）✗。
