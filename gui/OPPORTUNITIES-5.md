# 机会点清单（第 6 批）—— 依据画像与**已合并的真机事实**

> 排序依据同前：**她最可能先撞上哪个** × **撞上之后能不能自救**（画像第 3 条：**她出错时无法自救** ✗）。
> **纪律**：每条「现状」都先**核过仓库或真机报告** ✓；写「没有」的地方**真的 grep 过** ✓。
>
> **本批的起点是一条真机自我纠正** ✓ —— 第 5 批真机把 Narrator 驱动起来之后发现：
> 我们**算出来的**"读屏会念什么"**高估了**（详见 §已收口 的说明 ✓）。

---

## 已收口（第 5 批 ✓，7 个 PR）

| PR | 事 | 最值钱的地方 |
| --- | --- | --- |
| **#230** | 结果面板每行按钮名字带文件名 | 真机复验：**不同名字从 3 涨到 95** ✓ |
| **#231** | 「停下」之后说清结果 | 核对出**不支持"接着做"**→ **拒绝摆那个按钮** ✓ |
| **#232** | 她的环境四条 | 第 4 条**推翻了问题本身** ✓（产品不读系统主题 ✓）|
| **#233** | 真机闭环 + 本机 a11y 闸门 | **M=3 → 95** ✓、`runtimeId` 去重 ✓ |
| **#234** | 卡片上的术语给一句话 | 「宁可少不可滥」✓ |
| **#235** | 结果按今天/这周/更早分组 | 边界有理由 ✓（**这周从周一算起** ✓「中国习惯」✓、只分组不排序 ✓）|
| **#236** | 每份结果一个真标题 | **只加语义不改视觉** ✓（等 CI）|
| **#237** | 读屏**实际念出来**的（Narrator + ETW）| **纠正了我们自己的高估** ✓✓ |

### 第 5 批最重要的一条（**自我纠正** ✓✓）

`#237` 用 **ETW 抓到 Narrator 的 `SpokenText`** ✓（真念出来的文字 ✓）—— 然后发现：

> 我按 UIA 树算出来的「**整行 100 字列表项**」✓，**Narrator 一次都没念** ✓
> —— 因为 `ListItem` **不可 Tab** ✓，**她键盘上根本到不了那里** ✓。

**含义**（对后续每一轮都成立 ✓）：**"读屏会念什么"不能靠 UIA 树算** ✗ ——
**只有 Tab 到得了的东西才会被念** ✓。我们以后做 a11y 判断时，**先问"她键盘到得了吗"** ✓。

---

## 第 6 批在做

### P0 · 每份结果之间**不能按标题跳** ✗（#236 修了语义，真机还没复验）

**判据**：`-22.md`（§0 第 5 条 ✓，真机 ✓）：
> 面板里**有**标题（`标题级别 2` ✓），但**没有一条落在结果上** ✓；6 行结果只有"打开…/打开…所在的文件夹"两组按钮可 Tab ✓。

**现状**：`#236` 已加 `<h3>` + 可数清单 ✓（**本机文本闸门绿** ✓）—— **但真机没复验** ✗，
而且 **#236 的 CI 一直没触发** ✗（见下面那条流程问题 ✓）。

### P1 · 读屏念出来的**环境噪音** ✗（**不是我们的界面，但她在意**）

**判据**：`-22.md` §0 第 4 条 ✓（真机 ✓）：
> **画面扫描**（36 次 ✓）、**输入法状态**（"英语输入模式"✓）、**窗口标题**（把 `…\cante-gui.exe 窗口` 当名字念 ✓）。

**现状（我核过 ✓，并**修正了一条我原本的判断** ✗）**：
- **「窗口标题」那条，我一开始以为是我们的 `tauri.conf` 写错了** ✗ —— 核完发现 `title` **已经是「Cante」** ✓
  （`tauri.conf.json:15` ✓），**没有** `set_title` 覆盖它 ✓。真机念出来的 `…\cante-gui.exe 窗口` ✓
  是 **Narrator 对"窗口"元素的默认命名**（取自**进程名** ✓），**不是**我们的 title ✓；
- **含义**：这一条**我们改不了** ✗（除非改可执行文件名 ✗ —— 那是打包契约 ✓，动它代价大 ✓）。
  → 从"P1 可做"**降级为 P3「做不到，如实记」** ✓；
- **画面扫描 / 输入法状态** ✗ 同样是 Narrator 自身的功能 ✓（我们改不了 ✓）。

**做法**：**不做** ✗，但**写进文档** ✓ —— 让下次做读屏走查的人知道"这三条噪音不是我们的 bug" ✓
（否则每轮都会有人重新怀疑一遍 ✓）。

### P1 · 「我做的结果」里**一份都没有**时说什么 ✗

**现状（核过 ✓）**：`ResultsPanel.tsx` 有 `role="status"` 的一行 ✓（`:170` ✓）——
但**没核过**它在她**从没做过任何事**时说的是什么 ✓，以及**她说得出口吗** ✓。
真机报告也点过这条没验 ✗（`-19.md` §6：**面板只在有历史运行时才有内容** ✓）。
**做法**：先核文案 ✓，再在真机上造"空历史"那一面 ✓（Windows 侧已能清档案 ✓）。

### P2 · 她**连点两次**同一个按钮会怎样 ✗

**现状**：**没验过** ✗（`-19.md` / `-22.md` 都没有 ✓）。脆弱点很实在 ✓：
「打开文件」「打开所在文件夹」「再跑一次」——**连点**会不会开两个窗口 ✓、起两轮任务 ✗。
**做法**：真机上连点两次，看**窗口数**与**运行数** ✓（用已有的 Shell 数窗口法 ✓，`#220` 用过 ✓）。

### P2 · 卡片库里那一堆卡她怎么扫 ✗

**现状（核过 ✓）**：`TaskLibrary.tsx` 有搜索 ✓ + 分类 ✓（`catalog.ts` ✓）—— 但**没有**"**按她的话**"✓
（首页那句挑卡 ✓ 只在**首页** ✓，`#224` ✓；**卡片库里没有** ✗）。
**做法**：把「**说一句话帮我挑**」也放进卡片库 ✓（同一个 `suggestTasks` ✓，**不新造** ✓）。

### P3 · **键盘走完 95 步**太长了 ✗

**现状（核过 ✓）**：`-19.md` 量到 `results` 一场 **93 步** ✓。`#236` 加了标题之后，
**理论上**可以按标题跳 ✓ —— 但**能不能跳取决于读屏/浏览器实现** ✓，而我们**验不了** ✗。
**做法**：**不做** ✗ —— 等真机复验（`#236` 合完之后 ✓）有结论再说 ✓。**先别为它改设计** ✗。

---

## 一条**流程问题**（不是产品问题，但要记 ✓）

**GitHub 有一段时间不再为新推送创建 workflow run** ✗（`#236` 的分支 `total_count = 0` ✓，
同分支的**老 run 都在** ✓、workflow `active` ✓、`paths` 匹配 ✓、**带真改动的提交也不触发** ✗、
githubstatus **All Systems Operational** ✓、换成**新分支名**推**也不触发** ✗）。

→ 结论：**不是我们的配置** ✓。**按目标的门槛（合并前 CI 双平台绿）`#236` 先不合** ✓ ——
等它恢复（或换一种能触发的方式 ✓）。**这条写进 PR 留言了** ✓。

---

## 不做 ✗（同前几批边界）

不碰 `tauri.conf.json` 的打包段 / workflows / 专业模式 ✓；不加"新系统 / 新账号 / 上传数据" ✗；
不为了通过而放松任何闸门 ✗。
