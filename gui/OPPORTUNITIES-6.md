# 第 7 批机会点：**先把"没验过"变成"验过"**，再从画像里找新增量

起点：第 6 批 9 个 PR（#237–#246）+ #247 全部收口 ✓，**0 个开着 issue / 0 个开着 PR** ✓。
本清单**先核再写** ✓ —— 我逐个查了**六个**怀疑点 ✓，结果**两个成立、四个不成立** ✗。
**不成立的那四个也写在这里** ✓（下次别再重复怀疑一遍 ✓）。

---

## 已核：**不是缺口** ✗（省得下一轮再查一遍）

| 怀疑 | 核过的事实 | 结论 |
| --- | --- | --- |
| 结果名字撞车（第二个月做同样的事） | `capabilities.ts:107` ✓ 已经把规矩写进**生产指令** ✓：「`结果.xlsx` 必须是**还不存在**的新文件名…**不要反复重试同一个名字**：换一个新名字（例如加「-新」「-2」）」✓；`sheets.rs:300` ✓ 只新建、`AlreadyExists` 绝不覆盖 ✓；`copy-recovery.ts:94` ✓ 有「关掉那个窗口再试」✓ | **已有** ✓ |
| 打印 | `copy-print.ts` ✓ 已覆盖"结果在哪 + 怎么打印（Ctrl+P）"✓ | **已有** ✓ |
| 跑着的时候点别的卡 | `App.tsx` ✓ `active()` 一设上，**首页整个被换掉** ✓ → 她**点不到别的卡** ✓；`store.activeRun()` ✓ 也挡住定时/排队的并发 ✓ | **不是缺口** ✓ |
| 说「不对」想改一改 | `TaskRunner.tsx:676` ✓ 有可编辑的 `textarea` ✓ + 「就照这个例子来」✓；`copy-first-run.ts:133` ✓ 有字时不静默清掉 ✓ | **已有** ✓ |

---

## P0 · 定时任务的前提没说清 ✗（**新**）

**判据（核过 ✓）**：`gui/src/simple/copy-schedule.ts` 里**没有一个字**提"得开着应用" ✗
（我 grep 了 `开着`/`开应用`/`电脑开着`/`运行时`/`启动时` ✓ **全空** ✗）。
而 `store.ts:1519` `runScheduled` 是**应用内**的定时器 ✓（`checkDueSchedules` ✓）——
**应用没开就不会跑** ✗。

**为什么这是 P0**：她是**王姐** ✓ —— 她会以为"设了以后每天自动做"✓ 就等于**不用管** ✗。
一旦某天没跑（关着机 ✓），她**不会知道为什么** ✗（画像第 3 条：她出错时无法自救 ✗）。
`schedule.ts:118` 已经写了"关机一周回来最多补一次"✓ —— 但**这句话没有任何出口** ✗。

**要做**：① 在"以后自动做"那一屏**用一句话说清前提** ✓（"要这台电脑和应用都开着，到点才会做"✓）；
② **补跑过之后要说一声** ✓（"上次错过的，今天补上了"✓）；③ 一条断言钉住"这句话在生产路径上真的出现" ✓。

## P0 · 多个结果**怎么一起交出去** ✗（**新**）

**判据（核过 ✓）**：`ResultsPanel.tsx` ✓ / `copy-share.ts` ✓ 里**没有**"全选/一起复制/打包"类文案 ✗。
她一次做 5 份表 ✓（合并 + 核对 + 台账 ✓）→ 要**逐个点开、逐个复制** ✗。

**要做**：给一个"**把这批结果一次交出去**"的出口 ✓ —— 优先**复制成一段能贴进微信的文字** ✓
（复用 `share.ts` ✓，**不新造协议** ✗）；**打包成 zip** 只有在复制不够用时才做 ✓（先别做 ✗）。

## P1 · 撤销的**边界**（能撤几次、多久、撤不回来时说什么）✗

**判据**：`files.rs` ✓ 有备份 + 快照 + "undo 本身也可撤销"✓，但**面向她的话里没有"能撤几次/多久"** ✗。
她真正的恐惧是"**过两天发现错了，还能不能撤回来**"✓。

**要做**：先把**事实**核出来 ✓（备份保留策略 ✓），再决定是**说清** ✓ 还是**改行为** ✗（不预设答案 ✗）。

## P1 · 她的结果越攒越多 ✗

**判据**：结果都落在**她选的那个文件旁边** ✓（#90 约定 ✓）→ 她的桌面/文件夹会**慢慢堆满** ✗，
而"我做的结果"面板**只列不做别的** ✓（核过：没有清理/归档入口 ✗）。

**要做**：**先问该不该管** ✓ —— 大概率**不该我们删** ✗（红线：不弄坏她的东西 ✓）；
但有价值的是"**旧结果在哪儿、占了多少**"✓ 与"**这份是第几次做的**"✓ 能说清 ✓。

## P2 · Windows：**结果交付**这条路上的中文/空格路径 ✗

**判据**：#197 已验过**输入侧**的中文/空格/OneDrive ✓（`accept-paths.ps1` ✓）——
但**结果落点**是"**你选的那个文件旁边**"✓，那条路**没有单独量过** ✓（`run-results-audit.ps1` ✓
量的是**找不到时说什么** ✓，不是**落点** ✓）。

**要做**：Windows 侧在**纯中文 / 中文+空格 / OneDrive 重定向桌面**三种落点上各跑一轮 ✓，
核对"结果真在原件旁边 ✓、能读回 ✓、原件哈希不变 ✓"。

---

## 边界（不做 ✗）

不碰 `tauri.conf.json` 的打包段 / workflows / 专业模式 ✓；不加"新系统/新账号/上传数据"✗；
不为了通过而放松既有闸门（copy / typography / tone / platform / a11y / 秘密扫描）✗。
